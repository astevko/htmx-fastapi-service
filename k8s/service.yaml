apiVersion: v1
kind: Service
metadata:
  name: htmx-fastapi-service
  namespace: htmx-fastapi
  labels:
    app: htmx-fastapi-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: htmx-fastapi-service
