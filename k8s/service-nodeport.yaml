apiVersion: v1
kind: Service
metadata:
  name: htmx-fastapi-nodeport
  namespace: htmx-fastapi
  labels:
    app: htmx-fastapi-service
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: htmx-fastapi-service
